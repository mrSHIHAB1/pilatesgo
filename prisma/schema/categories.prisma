model Category {
  id          String     @id @default(uuid())
  name        String     @unique
  description String?
  createdAt   DateTime   @default(now())
  exercises   Exercise[]
  programs    Program[]
  videos      Video[]
  workouts    Workout[]
}
